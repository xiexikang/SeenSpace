# 内容灵感平台设计风格摘录

> 采样时间：2026-07-06
>
> 采样页面：首页、主题页与内容聚合页
>
> 数据来源：页面 **CSS computed styles** + 可见组件结构（非源代码级 token；偏“设计复刻”视角）。

## 视觉基调

- 图像优先：瀑布流/图卡作为主内容，文字做导航与辅助信息。
- 中性底色 + 强品牌点色：页面大面积白/浅灰，关键操作集中使用品牌红。
- 圆角偏克制：多数容器与图卡为直角或小圆角，按钮与少量控件采用胶囊形圆角，形成“强调”。

## 品牌与配色

> 以下色值均为采样页面中出现的真实值（rgb 转十六进制）。

| 语义 | 值 | 典型位置 |
| --- | --- | --- |
| 品牌主色 Brand | `#FF284B`（rgb(255,40,75)） | `立即登录`、`关注` 等主按钮；标题强调文字 |
| 主文字 Primary Text | `#222529`（rgb(34,37,41)） | 导航、正文、列表标题 |
| 次级文字 Secondary Text | `#7F8792`（rgb(127,135,146)） | 弱提示、辅助信息 |
| 三级文字 Tertiary Text | `#9DA3AC`（rgb(157,163,172)） | 更弱的注释、占位类信息 |
| 浅底色 Surface | `#F6F7F9`（rgb(246,247,249)） | 浮层/局部底、轻弱背景块 |
| 分隔/边框 Border | `#E8EAEC`（rgb(232,234,236)） | 下拉触发器边框、分隔线 |
| 主题图片底色（内容相关） | `#E3FF7F`（rgb(227,255,127)）等 | 图片/卡片自身内容色（不属于系统色板） |

> 备注：采样中还出现了 `#FF6A72`（rgb(255,106,114)）、`#CD0023`（rgb(205,0,35)）等偏红色值，更多像是品牌红的衍生/状态色或内容色，建议在更多页面补采样后再固化为完整 token。

## 字体

| 语义 | 字体栈 | 说明 |
| --- | --- | --- |
| 默认 UI 字体 | `"Alibaba Sans", -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", "Hiragino Sans GB", "Helvetica Neue", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"` | 中文优先覆盖齐全；整体偏现代、干净，适合图像平台的信息辅助文本 |

## 字号与字重

| 场景 | 字号 | 行高 | 字重 | 颜色 |
| --- | --- | --- | --- | --- |
| 页面默认（Body） | `14px` | `22px`（≈22.001px） | `400` | `#222529` |
| 搜索输入框文字 | `16px` | `25px`（≈25.144px） | `400` | `#222529` |
| 主按钮文字 | `14px` | `44px`（按钮高度同为 44） | `600` | `#FFFFFF` |
| 标签 chip | `12px` | `20px` | `400` | `#4C535C`（rgb(76,83,92)） |
| 首页品牌强调字 | `48px` | `75px`（≈75.432px） | `300` | `#FF284B` |

## 圆角与阴影

| --- | --- |
| 下拉触发器（如 `综合排序`） | `12px` |
| 标签 chip（如 `世界杯专题`） | `8px` |
| 其他采样到的常见值 | `2px`、`6px`、`20px`、`24px`、`26px`、`50%`（头像/图标圆形） |

### 阴影

采样页面出现的典型阴影（用于浮层/弹层/强调层级）：

- `rgba(0, 0, 0, 0.1) 0px 0px 1px 0px, rgba(0, 0, 0, 0.1) 0px 8px 40px -2px`
- `rgba(255, 255, 255, 0.24) 0px 0px 24px 0px inset`（偏“内发光”效果，通常用于高亮/浮层内部）

## 栅格与间距（从可见样式推断）

- 顶部导航左右内边距：`20px`（`padding: 0 20px`）。
- 主按钮内边距：`0 20px`（横向 padding），高度为 `44px`。
- 下拉触发器（`综合排序`）内边距：`0 24px 0 12px`（右侧更宽，为图标/箭头预留）。
- 标签 chip 内边距：`2px 8px`（紧凑型标签）。

## 组件规范（关键样式）

### 主按钮（Primary Button）

- 背景：`#FF284B`
- 文字：`#FFFFFF`
- 字重：`600`
- 高度/行高：`44px`
- 圆角：`44px`
- Padding：`0 20px`

适用：高频主行动（登录、关注、开通等）。

### 标签 chip（Tag）

- 字号/行高：`12px / 20px`
- 文字：`#4C535C`
- 边框：`1px solid rgba(30, 32, 35, 0.1)`
- 圆角：`8px`
- Padding：`2px 8px`

适用：主题/标签聚合导航。

### 下拉触发器（Dropdown Trigger）

- 背景：`#FFFFFF`
- 边框：`1px solid #E8EAEC`
- 圆角：`12px`
- Padding：`0 24px 0 12px`

适用：排序、筛选等弱强调控件。

### 输入框（Search Input）

- 字号：`16px`
- 背景：透明（容器背景/边框由外层控制）
- 文字：`#222529`
